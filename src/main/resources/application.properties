# DBCP(database connection pool) 설정
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.url=jdbc:h2:tcp://127.0.0.1/~/test
spring.datasource.username=sa
spring.datasource.password=

# jpa 설정
spring.jpa.hibernate.ddl-auto=create
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# 로그레벨 설정
logging.level.org.hibernate.SQL=debug

# 프로파일 설정
#spring.profiles.active=local

# devtools
spring.devtools.livereload.enabled = false

# 세션 inactive interval 30분으로 설정
server.servlet.session.timeout=30m

# https 설정 (키 스토어)
server.ssl.key-store=keystore.p12
server.ssl.key-store-password=123456
server.ssl.keyStoreType=PKCS12
server.ssl.keyAlias=mymall

# 포트 번호 설정(https) http:8080 혼동하지 말기!
server.port=8443

# google 메일 서비스 (587 구글측 포트번호)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username= kim595299@gmail.com
spring.mail.password= braoddjjsfatsfll
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.starttls.enable=true
